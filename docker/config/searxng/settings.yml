use_default_settings: true

general:
  instance_name: "HydeClaw Search"
  debug: false

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "ru"
  formats:
    - html
    - json

server:
  # Sentinel replaced at boot by the SEARXNG_SECRET env (wired from SEARXNG_SECRET_KEY in docker-compose).
  # No real secret is committed; set SEARXNG_SECRET_KEY in the server .env for a unique value.
  secret_key: "ultrasecretkey"
  limiter: false
  image_proxy: false
  port: 8080
  bind_address: "0.0.0.0"

ui:
  static_use_hash: true
  default_theme: simple
  theme_args:
    simple_style: dark

engines:
  - name: google
    engine: google
    shortcut: g

  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg

  - name: wikipedia
    engine: wikipedia
    shortcut: wp

  - name: github
    engine: github
    shortcut: gh
    disabled: true

  - name: stackoverflow
    engine: stackoverflow
    shortcut: so
    disabled: true
