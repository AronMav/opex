info: cargo-llvm-cov coverage snapshot (post-W1 refactor T4 acceptance)

Note: Windows platform with DATABASE_URL unset. Coverage run with 9 DB-dependent tests failing (EnvVar(NotPresent)).

Post-refactor coverage capture (same limitation as T0 baseline):
- Compiled: hydeclaw-core package (lib)
- Test execution: 59 passed, 9 failed (DB-dependent)
- Coverage data: Placeholder (llvm-cov failure on Windows without DATABASE_URL)
- Platform: Windows x86_64, Rust stable

Status: Post-refactor coverage frozen for T4 acceptance verification.
DB test failures are expected without DATABASE_URL set (as per CLAUDE.md).

Acceptance note: Coverage regression assessment not possible due to platform limitation.
Both T0 and T4 have same constraint; relative code structure changes validate via:
1. Test count parity (1279 passed, 31 failed — unchanged)
2. Extract integrity (no new failures in openai/ tree)
3. Clippy clean + rustls invariant maintained
