# Profiles Entity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ввести сущность «Профиль» (провайдеры + модели + голос + резервные цепочки для всех агентских capability), перевести агентов на `profile = "…"`, упразднить `provider_active` для всего, кроме embedding.

**Architecture:** Профили живут в новой таблице `profiles` (JSONB-слоты `{capability: [{provider, model?, voice?}, …]}`). Единый резолвер в core выдаёт цепочку по capability; engine строит primary из `text[0]` и ходит по резервам через обобщённый fallback-механизм; медиа/поиск получают явные `X-Opex-Provider` / `X-Opex-Voice` / `X-Opex-Providers` заголовки; toolgate остаётся «глупым». Однократный startup-seed переносит текущие настройки.

**Tech Stack:** Rust (axum, sqlx, serde), PostgreSQL 17, Python/FastAPI (toolgate), Next.js 16 + React Query + vitest.

**Spec:** [docs/superpowers/specs/2026-07-15-profiles-design.md](../specs/2026-07-15-profiles-design.md)

## Global Constraints

- Rust 2024, только rustls — никаких OpenSSL-зависимостей.
- Применённые миграции не редактировать (checksum); новая миграция — `migrations/084_profiles.sql`.
- Тесты opex-core живут в BIN-таргете: `cargo test -p opex-core --bin opex-core <фильтр>` (НЕ `--lib`). DB-тесты: `#[sqlx::test(migrations = "../../migrations")]`, нужен `DATABASE_URL` (локально `make test-db`, иначе они падают с `EnvVar(NotPresent)` — это ожидаемо).
- На Windows Rust-тесты не запускать (крашатся) — локально только `make check`; авторитетный прогон тестов на сервере/CI.
- После изменения любых `*Dto` с `#[cfg_attr(feature = "ts-gen", …)]`: `make gen-types` и коммит `ui/src/types/api.generated.ts` (CI ловит drift).
- vitest запускать только из `ui/` (`cd ui && npm test`), toolgate-тесты из `toolgate/` (`python -m pytest`).
- Коммиты без Co-Authored-By, работа в master, без push.
- Строковые константы capability: `text`, `compaction`, `stt`, `tts`, `vision`, `imagegen`, `websearch` — ровно эти написания (совпадают с категориями `providers.type`, кроме `text`, у которого legacy-синоним категории `llm`).

---

### Task 1: Таблица `profiles` + `db/profiles.rs` (типы, CRUD, валидация, copy)

**Files:**
- Create: `migrations/084_profiles.sql`
- Create: `crates/opex-core/src/db/profiles.rs`
- Modify: `crates/opex-core/src/db/mod.rs` (добавить `pub mod profiles;` — по образцу соседних `pub mod providers;`)

**Interfaces:**
- Produces (нужно всем последующим задачам):
  - `pub const DEFAULT_PROFILE: &str = "Default";`
  - `pub const PROFILE_CAPABILITIES: [&str; 7]`
  - `pub struct SlotEntry { pub provider: String, pub model: Option<String>, pub voice: Option<String> }`
  - `pub type Slots = std::collections::HashMap<String, Vec<SlotEntry>>;`
  - `pub struct ProfileRow { pub id: Uuid, pub name: String, pub slots: serde_json::Value, pub created_at: DateTime<Utc>, pub updated_at: DateTime<Utc> }` + `impl ProfileRow { pub fn parsed_slots(&self) -> Slots }`
  - `list_profiles`, `get_profile`, `get_profile_by_name`, `create_profile`, `update_profile`, `delete_profile`, `copy_profile`, `validate_slots` (сигнатуры в шаге 3)

- [ ] **Step 1: Миграция**

`migrations/084_profiles.sql`:

```sql
-- m084: profiles — именованные наборы провайдеров/моделей/голоса/резервов.
-- Слоты: {"text":[{"provider":"...","model":"..."}], "tts":[{"provider":"...","voice":"..."}], ...}
CREATE TABLE profiles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL UNIQUE,
    slots JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

- [ ] **Step 2: Failing-тесты**

В конец нового `crates/opex-core/src/db/profiles.rs` (модуль тестов пишется вместе с кодом, но сначала — только тесты + заглушки `todo!()`, чтобы увидеть падение):

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn slot(provider: &str) -> SlotEntry {
        SlotEntry { provider: provider.into(), model: None, voice: None }
    }

    #[sqlx::test(migrations = "../../migrations")]
    async fn crud_roundtrip(pool: sqlx::PgPool) {
        let mut slots = Slots::new();
        slots.insert("tts".into(), vec![SlotEntry { provider: "mm".into(), model: None, voice: Some("Champ".into()) }]);
        let created = create_profile(&pool, "P1", &slots).await.unwrap();
        assert_eq!(created.name, "P1");
        let fetched = get_profile_by_name(&pool, "P1").await.unwrap().unwrap();
        assert_eq!(fetched.parsed_slots()["tts"][0].voice.as_deref(), Some("Champ"));

        slots.insert("stt".into(), vec![slot("whisper")]);
        let updated = update_profile(&pool, created.id, Some("P1x"), Some(&slots)).await.unwrap().unwrap();
        assert_eq!(updated.name, "P1x");
        assert_eq!(updated.parsed_slots().len(), 2);

        assert!(delete_profile(&pool, created.id).await.unwrap());
        assert!(get_profile(&pool, created.id).await.unwrap().is_none());
    }

    #[sqlx::test(migrations = "../../migrations")]
    async fn copy_uniquifies_name(pool: sqlx::PgPool) {
        let p = create_profile(&pool, "Base", &Slots::new()).await.unwrap();
        let c1 = copy_profile(&pool, p.id).await.unwrap().unwrap();
        assert_eq!(c1.name, "Base (copy)");
        let c2 = copy_profile(&pool, p.id).await.unwrap().unwrap();
        assert_eq!(c2.name, "Base (copy 2)");
    }

    #[sqlx::test(migrations = "../../migrations")]
    async fn validate_rejects_bad_slots(pool: sqlx::PgPool) {
        // unknown capability
        let mut bad = Slots::new();
        bad.insert("smellgen".into(), vec![slot("x")]);
        assert!(validate_slots(&pool, &bad).await.is_err());
        // empty provider name
        let mut bad2 = Slots::new();
        bad2.insert("tts".into(), vec![slot("")]);
        assert!(validate_slots(&pool, &bad2).await.is_err());
        // provider not in table
        let mut bad3 = Slots::new();
        bad3.insert("tts".into(), vec![slot("ghost")]);
        assert!(validate_slots(&pool, &bad3).await.is_err());
    }

    #[sqlx::test(migrations = "../../migrations")]
    async fn validate_checks_category(pool: sqlx::PgPool) {
        sqlx::query("INSERT INTO providers (name, type, provider_type) VALUES ('tts-p','tts','minimax'),('llm-p','llm','openai_compat')")
            .execute(&pool).await.unwrap();
        let mut ok = Slots::new();
        ok.insert("tts".into(), vec![slot("tts-p")]);
        ok.insert("text".into(), vec![slot("llm-p")]);   // text принимает категории text|llm
        ok.insert("compaction".into(), vec![slot("llm-p")]);
        assert!(validate_slots(&pool, &ok).await.is_ok());

        let mut cross = Slots::new();
        cross.insert("tts".into(), vec![slot("llm-p")]); // категория-mismatch
        assert!(validate_slots(&pool, &cross).await.is_err());
    }
}
```

Примечание: если в реальной схеме `providers` есть другие NOT NULL-колонки — дополнить INSERT в тесте по фактической схеме (смотреть `migrations/` по слову `CREATE TABLE providers`).

- [ ] **Step 3: Реализация `db/profiles.rs`**

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::PgPool;
use std::collections::HashMap;
use uuid::Uuid;

pub const DEFAULT_PROFILE: &str = "Default";

/// Capability-ключи слотов. `text` принимает провайдеров категории text|llm,
/// остальные — категорию с тем же именем.
pub const PROFILE_CAPABILITIES: [&str; 7] =
    ["text", "compaction", "stt", "tts", "vision", "imagegen", "websearch"];

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct SlotEntry {
    pub provider: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub voice: Option<String>,
}

pub type Slots = HashMap<String, Vec<SlotEntry>>;

#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct ProfileRow {
    pub id: Uuid,
    pub name: String,
    pub slots: serde_json::Value,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

impl ProfileRow {
    /// Толерантный парс: битый JSONB → пустые слоты (лог на совести вызывающего).
    pub fn parsed_slots(&self) -> Slots {
        serde_json::from_value(self.slots.clone()).unwrap_or_default()
    }
}

pub async fn list_profiles(db: &PgPool) -> sqlx::Result<Vec<ProfileRow>> {
    sqlx::query_as("SELECT * FROM profiles ORDER BY name").fetch_all(db).await
}

pub async fn get_profile(db: &PgPool, id: Uuid) -> sqlx::Result<Option<ProfileRow>> {
    sqlx::query_as("SELECT * FROM profiles WHERE id = $1").bind(id).fetch_optional(db).await
}

pub async fn get_profile_by_name(db: &PgPool, name: &str) -> sqlx::Result<Option<ProfileRow>> {
    sqlx::query_as("SELECT * FROM profiles WHERE name = $1").bind(name).fetch_optional(db).await
}

pub async fn create_profile(db: &PgPool, name: &str, slots: &Slots) -> sqlx::Result<ProfileRow> {
    sqlx::query_as(
        "INSERT INTO profiles (name, slots) VALUES ($1, $2) RETURNING *")
        .bind(name)
        .bind(serde_json::to_value(slots).unwrap_or(serde_json::json!({})))
        .fetch_one(db).await
}

/// name/slots опциональны — None означает «не менять».
pub async fn update_profile(
    db: &PgPool, id: Uuid, name: Option<&str>, slots: Option<&Slots>,
) -> sqlx::Result<Option<ProfileRow>> {
    sqlx::query_as(
        "UPDATE profiles SET \
           name = COALESCE($2, name), \
           slots = COALESCE($3, slots), \
           updated_at = now() \
         WHERE id = $1 RETURNING *")
        .bind(id)
        .bind(name)
        .bind(slots.map(|s| serde_json::to_value(s).unwrap_or(serde_json::json!({}))))
        .fetch_optional(db).await
}

pub async fn delete_profile(db: &PgPool, id: Uuid) -> sqlx::Result<bool> {
    let res = sqlx::query("DELETE FROM profiles WHERE id = $1").bind(id).execute(db).await?;
    Ok(res.rows_affected() > 0)
}

/// Копия с уникализированным именем: "{name} (copy)", "{name} (copy 2)", …
pub async fn copy_profile(db: &PgPool, id: Uuid) -> sqlx::Result<Option<ProfileRow>> {
    let Some(src) = get_profile(db, id).await? else { return Ok(None) };
    let mut n = 1usize;
    loop {
        let candidate = if n == 1 { format!("{} (copy)", src.name) } else { format!("{} (copy {n})", src.name) };
        if get_profile_by_name(db, &candidate).await?.is_none() {
            let row = sqlx::query_as(
                "INSERT INTO profiles (name, slots) VALUES ($1, $2) RETURNING *")
                .bind(&candidate).bind(&src.slots).fetch_one(db).await?;
            return Ok(Some(row));
        }
        n += 1;
    }
}

/// Валидация слотов: известные capability, непустые имена, существование
/// записи providers подходящей категории. `text`/`compaction` принимают
/// категории text|llm; остальные — одноимённую категорию.
pub async fn validate_slots(db: &PgPool, slots: &Slots) -> Result<(), String> {
    for (cap, entries) in slots {
        if !PROFILE_CAPABILITIES.contains(&cap.as_str()) {
            return Err(format!("unknown capability '{cap}'"));
        }
        for e in entries {
            if e.provider.trim().is_empty() {
                return Err(format!("empty provider name in '{cap}' slot"));
            }
            let row = super::providers::get_provider_by_name(db, &e.provider)
                .await.map_err(|e| e.to_string())?
                .ok_or_else(|| format!("provider '{}' not found", e.provider))?;
            let ok = match cap.as_str() {
                "text" | "compaction" => row.category == "text" || row.category == "llm",
                other => row.category == other,
            };
            if !ok {
                return Err(format!(
                    "provider '{}' has category '{}', slot '{cap}' expects a matching category",
                    e.provider, row.category));
            }
        }
    }
    Ok(())
}
```

- [ ] **Step 4: Проверка**

Run: `make check` (Windows) и на сервере/CI: `cargo test -p opex-core --bin opex-core db::profiles -- --nocapture`
Expected: check чистый; 4 теста PASS.

- [ ] **Step 5: Commit**

```bash
git add migrations/084_profiles.sql crates/opex-core/src/db/profiles.rs crates/opex-core/src/db/mod.rs
git commit -m "feat(profiles): m084 profiles table + db CRUD/copy/validation"
```

---

### Task 2: Резолвер `agent/profile_resolver.rs`

**Files:**
- Create: `crates/opex-core/src/agent/profile_resolver.rs`
- Modify: `crates/opex-core/src/agent/mod.rs` (добавить `pub mod profile_resolver;` рядом с соседними mod-объявлениями)

**Interfaces:**
- Consumes: `db::profiles::{ProfileRow, SlotEntry, Slots, DEFAULT_PROFILE, get_profile_by_name}`, `db::providers::get_provider_by_name`
- Produces:
  - `pub async fn resolve_slots_for_agent(db: &PgPool, profile_name: &str, agent_name: &str) -> Slots` — профиль агента, при отсутствии → `Default` + warn, при отсутствии обоих → пустые слоты.
  - `pub async fn effective_chain(db: &PgPool, slots: &Slots, capability: &str) -> Vec<SlotEntry>` — цепочка слота с фильтром `enabled=true` записей провайдера (выключенная запись пропускается).

- [ ] **Step 1: Failing-тесты** (в том же файле, `#[cfg(test)]`)

```rust
#[sqlx::test(migrations = "../../migrations")]
async fn missing_profile_falls_back_to_default(pool: sqlx::PgPool) {
    let mut slots = crate::db::profiles::Slots::new();
    slots.insert("stt".into(), vec![crate::db::profiles::SlotEntry {
        provider: "w".into(), model: None, voice: None }]);
    crate::db::profiles::create_profile(&pool, "Default", &slots).await.unwrap();
    let got = resolve_slots_for_agent(&pool, "Ghost", "Arty").await;
    assert!(got.contains_key("stt"));
}

#[sqlx::test(migrations = "../../migrations")]
async fn no_profiles_at_all_gives_empty(pool: sqlx::PgPool) {
    let got = resolve_slots_for_agent(&pool, "Ghost", "Arty").await;
    assert!(got.is_empty());
}

#[sqlx::test(migrations = "../../migrations")]
async fn effective_chain_skips_disabled(pool: sqlx::PgPool) {
    sqlx::query("INSERT INTO providers (name, type, provider_type, enabled) VALUES \
        ('a','tts','minimax',false),('b','tts','minimax',true)")
        .execute(&pool).await.unwrap();
    let mut slots = crate::db::profiles::Slots::new();
    slots.insert("tts".into(), vec![
        crate::db::profiles::SlotEntry { provider: "a".into(), model: None, voice: None },
        crate::db::profiles::SlotEntry { provider: "b".into(), model: None, voice: None },
    ]);
    let chain = effective_chain(&pool, &slots, "tts").await;
    assert_eq!(chain.len(), 1);
    assert_eq!(chain[0].provider, "b");
}
```

- [ ] **Step 2: Реализация**

```rust
//! Резолюция профиля агента в цепочки провайдеров по capability.
//! Профиль не найден → Default + warn; нет и Default → пустые слоты
//! (агент без text-слота получает UnconfiguredProvider — существующий sentinel).

use crate::db::profiles::{Slots, SlotEntry, DEFAULT_PROFILE};
use sqlx::PgPool;

pub async fn resolve_slots_for_agent(db: &PgPool, profile_name: &str, agent_name: &str) -> Slots {
    match crate::db::profiles::get_profile_by_name(db, profile_name).await {
        Ok(Some(p)) => return p.parsed_slots(),
        Ok(None) => tracing::warn!(agent = %agent_name, profile = %profile_name,
            "agent profile not found; falling back to Default"),
        Err(e) => tracing::error!(agent = %agent_name, profile = %profile_name, error = %e,
            "profile lookup failed; falling back to Default"),
    }
    match crate::db::profiles::get_profile_by_name(db, DEFAULT_PROFILE).await {
        Ok(Some(p)) => p.parsed_slots(),
        _ => {
            tracing::error!(agent = %agent_name, "Default profile missing; all capabilities disabled");
            Slots::new()
        }
    }
}

/// Цепочка слота, отфильтрованная по `providers.enabled`. Пустой результат =
/// возможность выключена.
pub async fn effective_chain(db: &PgPool, slots: &Slots, capability: &str) -> Vec<SlotEntry> {
    let Some(entries) = slots.get(capability) else { return Vec::new() };
    let mut out = Vec::new();
    for e in entries {
        match crate::db::providers::get_provider_by_name(db, &e.provider).await {
            Ok(Some(row)) if row.enabled => out.push(e.clone()),
            Ok(Some(_)) => tracing::debug!(provider = %e.provider, capability, "slot entry disabled, skipping"),
            Ok(None) => tracing::warn!(provider = %e.provider, capability, "slot entry provider missing, skipping"),
            Err(err) => tracing::warn!(provider = %e.provider, capability, error = %err, "slot entry lookup failed, skipping"),
        }
    }
    out
}

```

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core profile_resolver`
Expected: 3 теста PASS.

```bash
git add crates/opex-core/src/agent/profile_resolver.rs crates/opex-core/src/agent/mod.rs
git commit -m "feat(profiles): profile resolver (agent -> slots -> enabled chain)"
```

---

### Task 3: `AgentSettings.profile` + legacy-поля в deprecated

**Files:**
- Modify: `crates/opex-core/src/config/mod.rs:929-959` (структура `AgentSettings`), и все места конструирования `AgentSettings { … }` в тестах этого файла (`:2665-2840`)

**Interfaces:**
- Produces: `AgentSettings.profile: String` (default `"Default"`), legacy-поля остаются читаемыми, но помечены deprecated.

- [ ] **Step 1: Изменение структуры**

В `AgentSettings`:

```rust
    /// Имя профиля (таблица profiles). Источник провайдеров/моделей/голоса.
    #[serde(default = "default_profile_name")]
    pub profile: String,
    // ── DEPRECATED (m084/profiles): читаются только startup-миграцией ────────
    // После прогона profiles-seed эти поля вычищаются из TOML. Engine и API
    // их не используют. Удалить поля целиком через один релиз.
    #[serde(default)]
    pub provider: String,
    #[serde(default)]
    pub model: String,
    #[serde(default)]
    pub provider_connection: Option<String>,
    #[serde(default)]
    pub fallback_provider: Option<String>,
    #[serde(default)]
    pub tts_provider: Option<String>,
    #[serde(default)]
    pub imagegen_provider: Option<String>,
```

и рядом с `default_language`:

```rust
fn default_profile_name() -> String {
    crate::db::profiles::DEFAULT_PROFILE.to_string()
}
```

Ключевое: `provider`/`model` были обязательными — становятся `#[serde(default)]` (старые TOML продолжают парситься, новые могут их не иметь).

- [ ] **Step 2: Тест на парс**

В существующий `#[cfg(test)]` блок config/mod.rs добавить:

```rust
#[test]
fn agent_toml_without_provider_fields_parses_with_default_profile() {
    let toml = r#"
[agent]
name = "X"
temperature = 1.0
"#;
    let cfg: crate::config::AgentConfig = toml::from_str(toml).unwrap();
    assert_eq!(cfg.agent.profile, "Default");
    assert_eq!(cfg.agent.provider, "");
}

#[test]
fn agent_toml_with_profile_field_parses() {
    let toml = "[agent]\nname = \"X\"\ntemperature = 1.0\nprofile = \"Custom\"\n";
    let cfg: crate::config::AgentConfig = toml::from_str(toml).unwrap();
    assert_eq!(cfg.agent.profile, "Custom");
}
```

(Точное имя обёртки — `AgentConfig` или аналог — взять из соседних TOML-тестов того же файла, например теста на `[[agent.routing]]` возле `config/mod.rs:3310`.)

- [ ] **Step 3: Починить конструкторы в тестах файла** — добавить `profile: default_profile_name(),` во все литералы `AgentSettings { … }` (строки ~2665-2840), `cargo check` покажет полный список.

- [ ] **Step 4: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core config::`
Expected: новые тесты PASS, старые не сломаны.

```bash
git add crates/opex-core/src/config/mod.rs
git commit -m "feat(profiles): AgentSettings.profile; legacy provider fields serde-tolerant deprecated"
```

---

### Task 4: Startup-seed миграция (Default + пер-агентные профили + переписывание TOML + чистка provider_active)

**Files:**
- Create: `crates/opex-core/src/db/profile_migration.rs`
- Modify: `crates/opex-core/src/db/mod.rs` (`pub mod profile_migration;`)
- Modify: `crates/opex-core/src/main.rs` — вызов после sqlx-миграций, рядом с существующим вызовом `run_upload_signature_migration` (найти по этому имени; тот же паттерн «однократный seed под sys_flags»)

**Interfaces:**
- Consumes: `opex_db::sys_flags::{try_get, …}` (образец: [upload_migration.rs:58-72](../../../crates/opex-core/src/db/upload_migration.rs); setter найти в `opex_db::sys_flags` — рядом с `try_get`), `db::profiles::*`, `db::providers::{list_provider_active, get_active_providers, set_provider_active_list}`, загрузка/запись агентских TOML (`toml_edit`, образец точечной правки — `config/mod.rs:2290-2308`).
- Produces: `pub async fn run_profiles_seed(db: &PgPool, agents_dir: &std::path::Path) -> anyhow::Result<()>` — идемпотентен (флаг `profiles_seed_v1`).

- [ ] **Step 1: Failing-тест** (DB-часть; TOML-часть тестируется через tempdir)

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[sqlx::test(migrations = "../../migrations")]
    async fn seed_builds_default_from_provider_active_and_cleans_up(pool: sqlx::PgPool) {
        sqlx::query("INSERT INTO providers (name, type, provider_type, enabled) VALUES \
            ('mm','tts','minimax',true),('sx','websearch','searxng',true),('emb','embedding','ollama',true),('llm1','llm','openai_compat',true)")
            .execute(&pool).await.unwrap();
        crate::db::providers::set_provider_active_list(&pool, "tts", &[("mm".into(), 1)]).await.unwrap();
        crate::db::providers::set_provider_active_list(&pool, "websearch", &[("sx".into(), 1)]).await.unwrap();
        crate::db::providers::set_provider_active_list(&pool, "embedding", &[("emb".into(), 1)]).await.unwrap();

        let dir = tempfile::tempdir().unwrap();
        std::fs::write(dir.path().join("Arty.toml"),
            "[agent]\nname = \"Arty\"\ntemperature = 1.0\nprovider = \"ollama\"\nmodel = \"kimi\"\nprovider_connection = \"llm1\"\nfallback_provider = \"llm1\"\n").unwrap();

        run_profiles_seed(&pool, dir.path()).await.unwrap();

        // Default создан из active
        let d = crate::db::profiles::get_profile_by_name(&pool, "Default").await.unwrap().unwrap();
        let slots = d.parsed_slots();
        assert_eq!(slots["tts"][0].provider, "mm");
        assert_eq!(slots["websearch"][0].provider, "sx");
        assert!(!slots.contains_key("embedding"));
        // text-слот Default взят из первого агента
        assert_eq!(slots["text"][0].provider, "llm1");
        assert_eq!(slots["text"][0].model.as_deref(), Some("kimi"));

        // TOML переписан
        let rewritten = std::fs::read_to_string(dir.path().join("Arty.toml")).unwrap();
        assert!(rewritten.contains("profile = "));
        assert!(!rewritten.contains("provider_connection"));
        assert!(!rewritten.contains("fallback_provider"));

        // provider_active: остался только embedding
        let rows = crate::db::providers::list_provider_active(&pool).await.unwrap();
        assert!(rows.iter().all(|r| r.capability == "embedding"));

        // идемпотентность
        run_profiles_seed(&pool, dir.path()).await.unwrap();
        assert_eq!(crate::db::profiles::list_profiles(&pool).await.unwrap().len(), 1);
    }
}
```

(`tempfile` уже в dev-dependencies воркспейса; если нет — добавить в `[dev-dependencies]` opex-core.)

- [ ] **Step 2: Реализация**

Алгоритм (все шаги в одной функции, ошибки — `anyhow::Context`, флаг ставится только в самом конце):

```rust
//! Однократный startup-seed профилей (m084). Идемпотентен через
//! sys_flags['profiles_seed_v1']. Порядок важен: сначала профили и TOML,
//! чистка provider_active и флаг — в самом конце.

use anyhow::Context;
use sqlx::PgPool;
use std::path::Path;

const FLAG: &str = "profiles_seed_v1";
/// capability, переезжающие в профиль (embedding остаётся).
const MIGRATED_CAPS: [&str; 6] = ["tts", "stt", "vision", "imagegen", "websearch", "compaction"];

pub async fn run_profiles_seed(db: &PgPool, agents_dir: &Path) -> anyhow::Result<()> {
    if opex_db::sys_flags::try_get(db, FLAG).await.context("profiles seed gate")?.is_some() {
        return Ok(());
    }

    // 1. Слоты Default из provider_active (все строки по priority).
    let mut default_slots = crate::db::profiles::Slots::new();
    for cap in MIGRATED_CAPS {
        let rows = crate::db::providers::get_active_providers(db, cap).await?;
        if rows.is_empty() { continue; }
        default_slots.insert(cap.to_string(), rows.into_iter()
            .map(|(name, _)| crate::db::profiles::SlotEntry { provider: name, model: None, voice: None })
            .collect());
    }

    // 2. Прочитать все config/agents/*.toml (toml_edit, сохраняя формат).
    //    Для каждого агента собрать text-цепочку из legacy-полей:
    //    [{provider_connection, model}] + [{fallback_provider}] (если заданы).
    //    Первый агент (base=true приоритетно, иначе первый по алфавиту) даёт
    //    text-слот Default.
    // 3. Если legacy-набор агента отличается от Default (text-цепочка или
    //    tts_provider/imagegen_provider заданы) — создать профиль {AgentName}:
    //    слоты = Default-слоты, поверх которых text из полей агента,
    //    tts/imagegen-override как одноэлементные цепочки. Иначе — Default.
    // 4. Переписать TOML: doc["agent"]["profile"] = value(имя);
    //    remove: provider, model, provider_connection, fallback_provider,
    //    tts_provider, imagegen_provider (через as_table_mut().remove(k) —
    //    образец: config/mod.rs:2290-2308).
    // 5. create_profile для Default и всех пер-агентных.
    // 6. Для cap in MIGRATED_CAPS: set_provider_active_list(db, cap, &[]).
    // 7. sys_flags set FLAG.
    ...
}
```

Пункты 2-4 писать по этому алгоритму дословно; ключевые вызовы: `std::fs::read_dir(agents_dir)` c фильтром `.toml`, `content.parse::<toml_edit::DocumentMut>()`, `doc["agent"]["profile"] = toml_edit::value(profile_name)`, чтение legacy-полей `doc["agent"]["provider_connection"].as_str()` и т.д., запись `std::fs::write(path, doc.to_string())`.

В `main.rs` — вызов сразу после существующей upload-миграции:

```rust
if let Err(e) = crate::db::profile_migration::run_profiles_seed(&db, std::path::Path::new(&agents_dir)).await {
    tracing::error!(error = %e, "profiles seed failed; will retry on next startup");
}
```

(`agents_dir` — тот же путь, из которого main загружает конфиги агентов; взять существующую переменную.)

- [ ] **Step 3: Проверка + Commit**

Run: сервер/CI `cargo test -p opex-core --bin opex-core profile_migration`; `make check`.

```bash
git add crates/opex-core/src/db/profile_migration.rs crates/opex-core/src/db/mod.rs crates/opex-core/src/main.rs
git commit -m "feat(profiles): one-shot startup seed (Default + per-agent, TOML rewrite, provider_active cleanup)"
```

---

### Task 5: Engine: слоты в конфиге движка; text[0] как primary; compaction из слота

**Files:**
- Modify: `crates/opex-core/src/gateway/handlers/agents/lifecycle.rs:20-130` (сборка движка)
- Modify: `crates/opex-core/src/agent/providers/factory.rs:171-226` (`resolve_provider_for_agent`)
- Modify: структура `EngineConfig` (найти по полю `compaction_provider` — определение рядом с `cfg().provider`; добавить поле `profile_slots`)

**Interfaces:**
- Consumes: `profile_resolver::{resolve_slots_for_agent, effective_chain}`
- Produces:
  - `EngineConfig.profile_slots: crate::db::profiles::Slots` — доступно как `engine.cfg().profile_slots` всем последующим задачам.
  - `resolve_provider_for_agent(db, agent, text_first: Option<&SlotEntry>, …)` — новый параметр вместо чтения `agent.provider_connection`.

- [ ] **Step 1: lifecycle — резолв слотов один раз при сборке движка**

В начале сборки (до создания провайдера):

```rust
let profile_slots = crate::agent::profile_resolver::resolve_slots_for_agent(
    &infra.db, &agent_cfg.agent.profile, name).await;
let text_chain = crate::agent::profile_resolver::effective_chain(
    &infra.db, &profile_slots, "text").await;
```

Primary-провайдер: ветка без routing вызывает `resolve_provider_for_agent(…, text_chain.first(), …)`. Ветка `routing` (не пустой `agent.routing`) остаётся как есть — routing по-прежнему overrides профиль (зафиксировать комментарием).

- [ ] **Step 2: factory — источник primary**

В `resolve_provider_for_agent` заменить блок `if let Some(conn_name) = agent.provider_connection…` на:

```rust
    if let Some(entry) = text_first {
        match crate::db::providers::get_provider_by_name(db, &entry.provider).await {
            Ok(Some(row)) if row.category == "text" || row.category == "llm" => {
                tracing::debug!(agent = %agent_name, provider = %entry.provider, "using profile text provider");
                let model_override = entry.model.as_deref().filter(|m| !m.is_empty());
                return resolve_provider_from_row(
                    &row, model_override, temperature, max_tokens, secrets, sandbox,
                    agent_name, workspace_dir, base, agent.prompt_cache,
                ).await;
            }
            Ok(Some(row)) => tracing::warn!(agent = %agent_name, provider = %entry.provider,
                category = %row.category, "profile text provider is not text/llm"),
            Ok(None) => tracing::warn!(agent = %agent_name, provider = %entry.provider,
                "profile text provider not found"),
            Err(e) => tracing::error!(agent = %agent_name, error = %e, "DB error resolving profile text provider"),
        }
    }
```

сигнатура: параметр `text_first: Option<&crate::db::profiles::SlotEntry>` вместо использования `agent.provider_connection`/`agent.model`. Тело sentinel-ветки не меняется.

- [ ] **Step 3: compaction из слота**

В lifecycle.rs:69-130 заменить источник имени:

```rust
// Было: get_provider_active(&infra.db, CAPABILITY_COMPACTION)
let compaction_entry = crate::agent::profile_resolver::effective_chain(
    &infra.db, &profile_slots, "compaction").await.into_iter().next();
```

Дальше существующий build-блок работает от `compaction_entry.map(|e| e.provider)`; при `model` в entry — прокинуть его как model_override в `build_provider`/`build_cli_provider` (второй аргумент `build_cli_provider(&provider_row, None, ctx)` — None сейчас; передать `entry.model.as_deref()`). Пустой слот → None → «falls back to primary» (существующее поведение).

`profile_slots` сохранить в `EngineConfig` (поле `pub profile_slots: crate::db::profiles::Slots`).

- [ ] **Step 4: Тест**

В `#[cfg(test)]` factory.rs (или соседний существующий тест-модуль провайдеров):

```rust
#[sqlx::test(migrations = "../../migrations")]
async fn profile_text_slot_resolves_named_provider(pool: sqlx::PgPool) {
    sqlx::query("INSERT INTO providers (name, type, provider_type, base_url, default_model, enabled) \
        VALUES ('llm1','llm','openai_compat','http://localhost:1','m-default',true)")
        .execute(&pool).await.unwrap();
    let entry = crate::db::profiles::SlotEntry { provider: "llm1".into(), model: Some("m-override".into()), voice: None };
    let agent = /* минимальный AgentSettings из test-хелпера config-тестов */;
    let p = resolve_provider_for_agent(&pool, &agent, Some(&entry), 1.0, None,
        /* secrets, sandbox, name, workspace, base — как в существующих тестах factory */).await;
    assert_ne!(p.name(), "unconfigured"); // точное имя sentinel взять из UnconfiguredProvider
}
```

(Хелперы взять из существующих тестов этого файла; если их нет — тестировать через уже существующий интеграционный тест lifecycle, каким покрывался provider_connection.)

- [ ] **Step 5: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core factory`

```bash
git add crates/opex-core/src/gateway/handlers/agents/lifecycle.rs crates/opex-core/src/agent/providers/factory.rs <файл EngineConfig>
git commit -m "feat(profiles): engine resolves text/compaction providers from profile slots"
```

---

### Task 6: Fallback-цепочка text[1..]

**Files:**
- Modify: `crates/opex-core/src/agent/pipeline/llm_call.rs:35-118` (`create_fallback_provider`)
- Modify: `crates/opex-core/src/agent/engine/loop_detector_integration.rs:29-40` (обёртка движка)
- Modify: `crates/opex-core/src/agent/pipeline/behaviour.rs` (`LayerState`, около :356)
- Modify: `crates/opex-core/src/agent/pipeline/execute.rs:432-468` (fallback-ветка)

**Interfaces:**
- Consumes: `EngineConfig.profile_slots` (Task 5)
- Produces:
  - `llm_call::create_fallback_provider(db, fallback_provider_name: Option<&str>, …)` — сигнатура НЕ меняется (по-прежнему один name); имя берётся из цепочки уровнем выше.
  - `engine.create_fallback_provider(chain_idx: usize) -> Option<Arc<dyn LlmProvider>>` — резолвит `text[1 + chain_idx]`.
  - `LayerState { …, fallback_chain_idx: usize }`.

- [ ] **Step 1: Обёртка движка**

`loop_detector_integration.rs:29`:

```rust
    /// Построить fallback-провайдера №idx из text-цепочки профиля
    /// (idx=0 → text[1], idx=1 → text[2], …). None = цепочка исчерпана.
    pub(crate) async fn create_fallback_provider(&self, chain_idx: usize)
        -> Option<Arc<dyn crate::agent::providers::LlmProvider>>
    {
        let chain = self.cfg().profile_slots.get("text")?;
        let entry = chain.get(1 + chain_idx)?;
        crate::agent::pipeline::llm_call::create_fallback_provider(
            &self.cfg().db,
            Some(entry.provider.as_str()),
            &self.cfg().agent.name,
            /* остальные аргументы — как в текущем вызове */
        ).await
    }
```

(Внутри `create_fallback_provider` model-override резерва: передавать `entry.model` невозможно текущей сигнатурой — резерв использует `default_model` записи, это уже задокументировано в llm_call.rs:79-81 и сохраняется. Если у entry задана model — добавить параметр `model_override: Option<&str>` в `llm_call::create_fallback_provider` и пробросить в `build_provider` через `ProviderOverrides { model: … }`; поле overrides смотреть в `resolve_provider_from_row`.)

- [ ] **Step 2: LayerState + execute-ветка**

`behaviour.rs` LayerState: добавить `pub fallback_chain_idx: usize` (инициализация 0 там же, где остальные поля).

`execute.rs:432-468` — генерализовать: сейчас `if !layer_state.using_fallback && …` свопает один раз. Новая логика:

```rust
                if let Some(ref fb_policy) = layers.fallback_provider {
                    layer_state.consecutive_failures += 1;
                    let failover_worthy =
                        crate::agent::error_classify::is_failover_worthy(&err_class);
                    if failover_worthy
                        || layer_state.consecutive_failures >= fb_policy.consecutive_failure_threshold
                    {
                        // Следующий элемент text-цепочки (первый вызов — text[1]).
                        let next = engine
                            .create_fallback_provider(layer_state.fallback_chain_idx)
                            .await;
                        if let Some(p) = next {
                            layer_state.fallback_provider = Some(p);
                            layer_state.using_fallback = true;
                            layer_state.fallback_chain_idx += 1;
                            layer_state.consecutive_failures = 0;
                            tracing::warn!(
                                agent = %engine.cfg().agent.name,
                                iteration,
                                chain_idx = layer_state.fallback_chain_idx,
                                error_class = ?err_class,
                                error = %e,
                                "switching to fallback provider after LLM error"
                            );
                            let _ = sink
                                .emit(PipelineEvent::Stream(StreamEvent::StepFinish {
                                    step_id,
                                    finish_reason: "fallback_switch".into(),
                                }))
                                .await;
                            continue;
                        }
                        // Цепочка исчерпана — падаем в общий error-путь ниже.
                    }
                }
```

Семантика сохранена и расширена: раньше `using_fallback=true` блокировал повторный своп; теперь каждый failover-worthy сбой на текущем провайдере двигает индекс дальше, `None` (исчерпание) → прежний error-путь. Per-run: `LayerState` создаётся на ход, следующий run снова с primary.

- [ ] **Step 3: Тест**

В тест-модуле behaviour.rs / execute.rs (где живут существующие fallback-тесты — grep `fallback layer present`, behaviour.rs:427) добавить сценарий цепочки: mock-двигатель с text-цепочкой из 3 записей, primary и резерв №1 возвращают transport-ошибку → к третьему провайдеру; проверить `fallback_chain_idx == 2`. Использовать существующую тест-инфраструктуру этих модулей (там уже есть фейковые провайдеры для fallback-тестов — переиспользовать их конструкторы).

- [ ] **Step 4: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core fallback`

```bash
git add crates/opex-core/src/agent/pipeline/{llm_call.rs,behaviour.rs,execute.rs} crates/opex-core/src/agent/engine/loop_detector_integration.rs
git commit -m "feat(profiles): text fallback walks profile chain instead of single fallback_provider"
```

---

### Task 7: Capability-тулы гейтятся слотами профиля

**Files:**
- Modify: `crates/opex-core/src/agent/capability_tools.rs:111-150` + тесты `:195-245`
- Modify: `crates/opex-core/src/agent/context_builder.rs:193` (трейт-метод) и его реализация (grep `capability_tool_defs` по crate)
- Modify: вызов `find_capability_tool` / `resolve_tool` в `engine_dispatch.rs` (grep `resolve_tool(`)

**Interfaces:**
- Produces:
  - `pub fn capability_tool_defs(slots: &Slots) -> Vec<YamlToolDef>` (синхронно — DB больше не нужна)
  - `pub fn find_capability_tool(slots: &Slots, name: &str) -> Option<YamlToolDef>`
  - `pub fn resolve_tool(workspace_dir: &str, slots: &Slots, db: …, name: &str)` — capability-ветка от слотов
  - Маппинг: `CapabilitySpec.capability` `"websearch"→"websearch"`, `"tts"→"tts"` и т.д. — имена уже совпадают со слотами.

- [ ] **Step 1: Переписать гейтинг**

```rust
pub fn capability_tool_defs(slots: &crate::db::profiles::Slots) -> Vec<YamlToolDef> {
    let mut out = Vec::new();
    for spec in capability_specs() {
        let Some(entry) = slots.get(spec.capability).and_then(|v| v.first()) else { continue };
        match parse_spec(spec) {
            Ok(def) => out.push(with_provider(def, &entry.provider)),
            Err(e) => tracing::error!(tool = spec.tool_name, error = %e, "capability spec parse failed"),
        }
    }
    out
}

pub fn find_capability_tool(slots: &crate::db::profiles::Slots, name: &str) -> Option<YamlToolDef> {
    let spec = capability_specs().iter().find(|s| s.tool_name == name)?;
    let entry = slots.get(spec.capability)?.first()?;
    parse_spec(spec).ok().map(|d| with_provider(d, &entry.provider))
}
```

Тесты `:195-245` переписать с `set_provider_active_list` на построение `Slots` in-memory (это убирает `#[sqlx::test]` — тесты становятся синхронными `#[test]`).

- [ ] **Step 2: Прокинуть слоты по вызовам** — `context_builder.rs:193` и реализация трейта берут `self.cfg().profile_slots`; `resolve_tool` в engine_dispatch — то же. `cargo check` покажет всех потребителей.

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core capability_tools`

```bash
git add crates/opex-core/src/agent/capability_tools.rs crates/opex-core/src/agent/context_builder.rs crates/opex-core/src/agent/engine_dispatch.rs
git commit -m "feat(profiles): capability tools gated by profile slots (no provider_active)"
```

---

### Task 8: Медиа-заголовки из профиля + голос + ретрай по цепочке (background media)

> Отклонение от спеки §4.3 (осознанное): для tool-пути (`synthesize_speech` через yaml-executor) голос передаётся заголовком `X-Opex-Voice`, а не инъекцией в body_template — инъекция в шаблон хрупкая. Toolgate читает заголовок при пустом body.voice (Task 10). Для `/api/tts/synthesize` голос идёт в теле, как в спеке (Task 9).

**Files:**
- Modify: `crates/opex-core/src/agent/pipeline/media_background.rs:205-300, 383+` (+тесты `:745-800`)
- Modify: `crates/opex-core/src/agent/pipeline/channel_actions.rs:160-180`

**Interfaces:**
- Consumes: `EngineConfig.profile_slots`
- Produces:
  - `pub fn provider_attempts_for(kind: MediaKind, slots: &Slots) -> Vec<Vec<(String, String)>>` — упорядоченные наборы заголовков, по одному на попытку: `[("X-Opex-Provider", p)]` (+ `("X-Opex-Voice", v)` для Voice при заданном voice).
  - `BackgroundMediaTask.provider_attempts: Vec<Vec<(String, String)>>` (замена смысла `tool_headers` для media-kind; не-media заголовки остаются как есть).
  - `pub fn is_provider_retryable(status: Option<reqwest::StatusCode>) -> bool`

- [ ] **Step 1: Failing-тесты** (рядом с существующими `provider_header_*`-тестами :745+)

```rust
#[test]
fn attempts_voice_chain_with_voice_header() {
    let mut slots = crate::db::profiles::Slots::new();
    slots.insert("tts".into(), vec![
        crate::db::profiles::SlotEntry { provider: "mm".into(), model: None, voice: Some("Champ".into()) },
        crate::db::profiles::SlotEntry { provider: "silero".into(), model: None, voice: None },
    ]);
    let a = provider_attempts_for(MediaKind::Voice, &slots);
    assert_eq!(a.len(), 2);
    assert!(a[0].contains(&("X-Opex-Provider".into(), "mm".into())));
    assert!(a[0].contains(&("X-Opex-Voice".into(), "Champ".into())));
    assert_eq!(a[1], vec![("X-Opex-Provider".to_string(), "silero".to_string())]);
}

#[test]
fn attempts_photo_never_reads_tts_slot() {
    let mut slots = crate::db::profiles::Slots::new();
    slots.insert("tts".into(), vec![crate::db::profiles::SlotEntry { provider: "mm".into(), model: None, voice: None }]);
    assert!(provider_attempts_for(MediaKind::Photo, &slots).is_empty());
}

#[test]
fn retryable_statuses() {
    assert!(is_provider_retryable(None));                                        // сеть/timeout
    assert!(is_provider_retryable(Some(reqwest::StatusCode::BAD_GATEWAY)));
    assert!(is_provider_retryable(Some(reqwest::StatusCode::SERVICE_UNAVAILABLE)));
    assert!(is_provider_retryable(Some(reqwest::StatusCode::TOO_MANY_REQUESTS))); // 429
    assert!(!is_provider_retryable(Some(reqwest::StatusCode::BAD_REQUEST)));      // 4xx
}
```

- [ ] **Step 2: Реализация**

`provider_header_for` заменяется на:

```rust
pub fn provider_attempts_for(
    kind: MediaKind,
    slots: &crate::db::profiles::Slots,
) -> Vec<Vec<(String, String)>> {
    let cap = match kind {
        MediaKind::Voice => "tts",
        MediaKind::Photo => "imagegen",
        MediaKind::Video | MediaKind::Other => return Vec::new(),
    };
    slots.get(cap).map(|chain| chain.iter().map(|e| {
        let mut h = vec![("X-Opex-Provider".to_string(), e.provider.clone())];
        if kind == MediaKind::Voice {
            if let Some(v) = e.voice.as_deref().filter(|v| !v.is_empty()) {
                h.push(("X-Opex-Voice".to_string(), v.to_string()));
            }
        }
        h
    }).collect()).unwrap_or_default()
}

/// Сетевые ошибки и 5xx/429 — пробуем следующий провайдер цепочки; прочие 4xx — нет.
pub fn is_provider_retryable(status: Option<reqwest::StatusCode>) -> bool {
    match status {
        None => true,
        Some(s) => s.is_server_error() || s == reqwest::StatusCode::TOO_MANY_REQUESTS,
    }
}
```

`from_ctx`: `provider_attempts_for(kind, &ctx.cfg.profile_slots)` вместо чтения `agent.tts_provider`/`agent.imagegen_provider`; поле задачи — `provider_attempts`. В `run()` найти единственный вызов HTTP-исполнения yaml-тула (он получает `self.tool_headers`) и обернуть:

```rust
let attempts: Vec<Vec<(String, String)>> = if self.provider_attempts.is_empty() {
    vec![Vec::new()] // media-kind без слота сюда не попадает (тул не зарегистрирован), но защищаемся
} else { self.provider_attempts.clone() };
let mut last_err = None;
for (i, headers) in attempts.iter().enumerate() {
    match /* существующий вызов, headers вместо self.tool_headers */ {
        Ok(bytes) => { /* существующий success-путь */ break-или-return; }
        Err(e) => {
            let status = /* extract HTTP status from error, None для transport */;
            if i + 1 < attempts.len() && is_provider_retryable(status) {
                tracing::warn!(kind = ?self.kind, attempt = i, error = %e,
                    "media provider failed; trying next in profile chain");
                last_err = Some(e);
                continue;
            }
            /* существующий error-путь с e */
        }
    }
}
```

Точную форму встроить в фактический код `run()` (существующие success/error-ветки не дублировать, а вынести из цикла как сейчас). `channel_actions.rs:170` — тот же переход на `provider_attempts_for` (там синхронный inline-путь: используется `attempts[0]`, ретрай не добавлять — inline-путь короткоживущий, ретраится самим LLM; зафиксировать комментарием).

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core media_background`

```bash
git add crates/opex-core/src/agent/pipeline/{media_background.rs,channel_actions.rs}
git commit -m "feat(profiles): media provider chain + voice header from profile slots"
```

---

### Task 9: `/api/tts/synthesize?agent=` + 409 + обход цепочки

**Files:**
- Modify: `crates/opex-core/src/gateway/handlers/config.rs:98-160` (`api_tts_synthesize`) и struct `TtsSynthesizeRequest` (в том же файле ниже)

**Interfaces:**
- Consumes: `profile_resolver::{resolve_slots_for_agent, effective_chain}`, `AgentCore` (есть доступ к db через `agents.deps`/engine cfg — взять `infra`-кластер как в других handlers: добавить `State(infra): State<InfraServices>` параметром).
- Produces: контракт для спеки №2 — `POST /api/tts/synthesize?agent=Arty {text}` → аудио; `409 {"error":"tts_disabled"}` при пустом слоте; body.voice/body.model остаются ручными override'ами (приоритет выше профиля).

- [ ] **Step 1: Реализация**

```rust
pub(crate) async fn api_tts_synthesize(
    State(agents): State<AgentCore>,
    State(infra): State<InfraServices>,
    axum::extract::Query(params): axum::extract::Query<std::collections::HashMap<String, String>>,
    Json(body): Json<TtsSynthesizeRequest>,
) -> impl IntoResponse {
    // … существующий резолв toolgate_url …

    // Цепочка из профиля агента (?agent=); без параметра — legacy-поведение
    // (пустая цепочка → один запрос без X-Opex-Provider, toolgate решает сам).
    let chain: Vec<crate::db::profiles::SlotEntry> = match params.get("agent") {
        Some(agent_name) => {
            let profile_name = agents.get_engine(agent_name).await
                .map(|e| e.cfg().agent.profile.clone())
                .unwrap_or_else(|| crate::db::profiles::DEFAULT_PROFILE.to_string());
            let slots = crate::agent::profile_resolver::resolve_slots_for_agent(
                &infra.db, &profile_name, agent_name).await;
            let chain = crate::agent::profile_resolver::effective_chain(&infra.db, &slots, "tts").await;
            if chain.is_empty() {
                return (StatusCode::CONFLICT,
                    Json(json!({"error": "tts_disabled", "hint": "profile has no tts slot"}))).into_response();
            }
            chain
        }
        None => Vec::new(),
    };

    let client = TOOLGATE_CLIENT.get_or_init(reqwest::Client::new);
    let attempts: Vec<Option<&crate::db::profiles::SlotEntry>> =
        if chain.is_empty() { vec![None] } else { chain.iter().map(Some).collect() };

    let mut last_status = StatusCode::BAD_GATEWAY;
    for (i, entry) in attempts.iter().enumerate() {
        // voice: body.voice (ручной override) > entry.voice (профиль) > "" (default записи)
        let voice = body.voice.clone().filter(|v| !v.is_empty())
            .or_else(|| entry.and_then(|e| e.voice.clone()))
            .unwrap_or_default();
        let mut payload = serde_json::json!({ "input": body.text, "voice": voice });
        if let Some(model) = body.model.clone().filter(|m| !m.is_empty()) {
            payload["model"] = serde_json::Value::String(model);
        }
        let mut req = client
            .post(format!("{}/v1/audio/speech", base.trim_end_matches('/')))
            .json(&payload);
        if let Some(e) = entry {
            req = req.header("X-Opex-Provider", &e.provider);
        }
        match req.send().await {
            Ok(r) if r.status().is_success() => {
                /* существующий success-путь: content-type + bytes */
            }
            Ok(r) => {
                last_status = StatusCode::from_u16(r.status().as_u16()).unwrap_or(StatusCode::BAD_GATEWAY);
                let retryable = r.status().is_server_error() || r.status().as_u16() == 429;
                if retryable && i + 1 < attempts.len() { continue; }
                /* существующий error-путь */
            }
            Err(_) if i + 1 < attempts.len() => continue,
            Err(e) => { /* существующий error-путь */ }
        }
    }
    ...
}
```

Существующие success/error-ветки переиспользовать (вынести формирование ответа как сейчас). `TtsSynthesizeRequest` не меняется (text, voice?, model?).

- [ ] **Step 2: Тест** — юнит на выбор voice-приоритета выносится как чистая функция:

```rust
fn effective_voice(body_voice: Option<&str>, slot_voice: Option<&str>) -> String {
    body_voice.filter(|v| !v.is_empty()).or(slot_voice.filter(|v| !v.is_empty())).unwrap_or("").to_string()
}

#[test]
fn voice_priority_body_over_slot_over_default() {
    assert_eq!(effective_voice(Some("manual"), Some("prof")), "manual");
    assert_eq!(effective_voice(None, Some("prof")), "prof");
    assert_eq!(effective_voice(Some(""), None), "");
}
```

и использовать её в handler-е. 409-путь проверяется E2E в Task 15 (финальная верификация).

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core tts`

```bash
git add crates/opex-core/src/gateway/handlers/config.rs
git commit -m "feat(profiles): /api/tts/synthesize resolves agent profile chain, 409 when tts slot empty"
```

---

### Task 10: Toolgate — `X-Opex-Voice`, `X-Opex-Providers` для поиска, категорийный фоллбек реестра

**Files:**
- Modify: `toolgate/routers/speech.py` (роутер `/v1/audio/speech` — найти по строке `audio/speech`)
- Modify: `toolgate/routers/search.py`
- Modify: `toolgate/registry.py:229-238`
- Test: `toolgate/tests/test_profiles_routing.py` (новый)

**Interfaces:**
- Produces:
  - `/v1/audio/speech`: приоритет голоса `body.voice (непустой) > X-Opex-Voice > default провайдера` (существующее поведение пустого voice).
  - `/v1/search`: `X-Opex-Providers: a,b,c` → перебор по порядку до первого успеха; без заголовка — прежний путь.
  - `Registry.aget_active`: нет active-строки → первый enabled-провайдер той же категории (сортировка по id) — сохраняет работу ctx.* после чистки provider_active.

- [ ] **Step 1: Failing-тесты**

```python
# toolgate/tests/test_profiles_routing.py
import pytest
from registry import Registry  # фактический класс/конструктор смотреть в registry.py

@pytest.mark.asyncio
async def test_aget_active_falls_back_to_category(monkeypatch, make_registry):
    # make_registry — фикстура по образцу существующих registry-тестов
    # (grep "def make_registry" / аналogichные конструкции в toolgate/tests).
    reg = make_registry(providers={
        "tts-b": {"type": "tts", "driver": "minimax", "enabled": True},
        "tts-a": {"type": "tts", "driver": "minimax", "enabled": True},
        "off":   {"type": "tts", "driver": "minimax", "enabled": False},
    }, active={})
    p = await reg.aget_active("tts")
    assert p is not None            # категорийный фоллбек
    # порядок: sorted by id → "tts-a"

@pytest.mark.asyncio
async def test_search_tries_provider_chain_in_header_order(search_client_with_two_providers):
    # первый провайдер кидает исключение → результат от второго
    resp = await search_client_with_two_providers.post(
        "/v1/search", json={"query": "q"},
        headers={"X-Opex-Providers": "bad,good"})
    assert resp.status_code == 200
```

(Фикстуры строить по образцу существующих toolgate-тестов — например `test_stt_openai.py` для httpx-моков и любых registry-тестов; если registry-фикстур нет — конструировать Registry напрямую с подмешанными `_instances`/`config`.)

- [ ] **Step 2: registry.py**

```python
    async def aget_active(self, capability: str) -> Provider | None:
        await self._refresh()
        with self._lock:
            active_id = self.config.active.get(capability)
            if active_id and active_id in self._instances:
                return self._instances[active_id]
            # profiles-мир: active-строк больше нет (кроме embedding) —
            # берём первый enabled-провайдер той же категории (id-порядок).
            for pid in sorted(self._instances):
                pcfg = self.config.providers.get(pid)
                if pcfg is not None and pcfg.type == capability:
                    return self._instances[pid]
            return None
```

- [ ] **Step 3: search.py**

```python
    registry = request.app.state.registry
    header_chain = [s.strip() for s in
                    (request.headers.get("x-opex-providers") or "").split(",") if s.strip()]
    candidates = []
    if provider_name:
        candidates = [await registry.aget_instance(provider_name)]
    elif header_chain:
        candidates = [await registry.aget_instance(n) for n in header_chain]
    else:
        candidates = [await registry.aget_active("websearch")]
    candidates = [c for c in candidates if c is not None]
    if not candidates:
        return JSONResponse(status_code=503, content={
            "error": "no_websearch_provider",
            "hint": "configure a web-search provider in Core UI",
        })
    http = request.app.state.http_client
    last_exc = None
    for provider in candidates:
        try:
            results = await provider.search(http, query, max_results)
            return {"results": results}
        except Exception as e:  # noqa: BLE001 — перебор цепочки
            log.warning("search provider %s failed: %s", getattr(provider, "name", "?"), e)
            last_exc = e
    log.exception("web search failed (all providers)", exc_info=last_exc)
    return JSONResponse(status_code=502, content={"error": "web search failed"})
```

- [ ] **Step 4: speech-роутер** — в начале обработчика `/v1/audio/speech`: если voice из тела пуст, взять `request.headers.get("x-opex-voice") or ""` (точное имя поля тела смотреть в самом роутере — OpenAI-compat `voice`).

- [ ] **Step 5: Проверка + Commit**

Run: `cd toolgate && python -m pytest tests/test_profiles_routing.py -v`
Expected: PASS.

```bash
git add toolgate/registry.py toolgate/routers/search.py toolgate/routers/speech.py toolgate/tests/test_profiles_routing.py
git commit -m "feat(profiles): toolgate X-Opex-Providers search chain, X-Opex-Voice, category fallback in registry"
```

---

### Task 11: Core: `X-Opex-Providers` для search_web

**Files:**
- Modify: `crates/opex-core/src/agent/engine_dispatch.rs` — место, где выполняются capability/yaml-тулы и собираются заголовки запроса (найти по `X-Opex-Provider` / `tool_headers` в этом файле; header-инъекция для search должна жить рядом)

**Interfaces:**
- Consumes: `EngineConfig.profile_slots`
- Produces: вызов `search_web` уходит в toolgate с `X-Opex-Providers: p1,p2,…` из слота `websearch`.

- [ ] **Step 1: Инъекция**

В точке сборки заголовков перед HTTP-вызовом yaml-тула:

```rust
if tool.name == "search_web" {
    let chain = self.cfg().profile_slots.get("websearch")
        .map(|v| v.iter().map(|e| e.provider.as_str()).collect::<Vec<_>>().join(","))
        .unwrap_or_default();
    if !chain.is_empty() {
        headers.push(("X-Opex-Providers".to_string(), chain));
    }
}
```

- [ ] **Step 2: Тест** — юнит на построение строки цепочки (вынести helper `websearch_header(slots) -> Option<(String,String)>` и покрыть `#[test]`: 2 провайдера → "a,b"; пустой слот → None).

- [ ] **Step 3: Проверка + Commit**

```bash
git add crates/opex-core/src/agent/engine_dispatch.rs
git commit -m "feat(profiles): search_web carries X-Opex-Providers chain from profile"
```

---

### Task 12: API `/api/profiles` (CRUD + copy + guard) и hot-reload агентов

**Files:**
- Create: `crates/opex-core/src/gateway/handlers/profiles.rs`
- Modify: `crates/opex-core/src/gateway/mod.rs` (или `handlers/mod.rs` — где `.merge()` соседних роутеров)

**Interfaces:**
- Consumes: `db::profiles::*`, список агентов и их `profile` (конфиги агентов — через тот же state, что `agents.rs` использует для чтения конфигов), механизм перезагрузки движка агента (существующий путь, которым PUT /api/agents применяет новый конфиг, — grep в `agents/crud.rs` вызов пересоздания/reload движка; переиспользовать функцию).
- Produces: REST по спеке §5 (см. таблицу спеки), JSON-форматы:
  - GET /api/profiles → `{"profiles": [{id, name, slots, agents: ["Arty"], created_at, updated_at}]}`
  - POST /api/profiles `{name, slots}` → 201 + строка; 400 на invalid slots/dup name
  - PUT /api/profiles/{id} `{name?, slots?}` → 200 + строка (+hot-reload затронутых агентов); Default: смена name → 400
  - POST /api/profiles/{id}/copy → 201 + строка
  - DELETE /api/profiles/{id} → 204; 409 `{error, agents}` если используется; Default → 400

- [ ] **Step 1: Failing-тесты** (`#[sqlx::test]` в том же файле; хендлеры тестировать на уровне db-функций + чистых guard-функций, полный axum-стек не поднимать — так делают соседние handler-тесты)

```rust
#[test]
fn default_profile_is_protected() {
    assert!(is_protected_profile("Default"));
    assert!(!is_protected_profile("Custom"));
}
```

- [ ] **Step 2: Реализация** — sub-router по образцу `providers.rs` (`pub(crate) fn routes() -> Router<AppState>`):

```rust
pub(crate) fn routes() -> Router<AppState> {
    Router::new()
        .route("/api/profiles", get(api_list_profiles).post(api_create_profile))
        .route("/api/profiles/{id}", get(api_get_profile).put(api_update_profile).delete(api_delete_profile))
        .route("/api/profiles/{id}/copy", post(api_copy_profile))
}

fn is_protected_profile(name: &str) -> bool { name == crate::db::profiles::DEFAULT_PROFILE }
```

Ключевые моменты реализации:
- `agents` в списке: пройтись по загруженным конфигам агентов (та же структура, из которой `agents.rs` строит список) и собрать `cfg.agent.profile == row.name`.
- POST/PUT: `validate_slots` перед записью; ошибка → 400 `{error}`. PUT для Default с `name != "Default"` → 400.
- DELETE: если `agents` непуст → 409 `{"error":"profile_in_use","agents":[…]}`; Default → 400.
- Hot-reload после PUT: для каждого агента с этим профилем вызвать существующий reload-путь (тот же вызов, что в конце PUT /api/agents в `crud.rs` — найти и переиспользовать; если он приватный — `pub(crate)`).
- Зарегистрировать `.merge(profiles::routes())` в композиции роутеров.

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core profiles`

```bash
git add crates/opex-core/src/gateway/handlers/profiles.rs crates/opex-core/src/gateway/handlers/mod.rs
git commit -m "feat(profiles): /api/profiles CRUD + copy + in-use guard + agent hot-reload"
```

---

### Task 13: Agents API: `profile` вместо шести полей + `capabilities` в summary + gen-types

**Files:**
- Modify: `crates/opex-core/src/gateway/handlers/agents/dto_structs.rs:181-220` (`AgentDetailDto`), схема create/update `agents/schema.rs:40-60, 190-210, 360-370`, `agents/dto.rs:45-55`, `agents/crud.rs:690-710, 1280-1300` (merge-хелперы legacy-полей)
- Modify: `crates/opex-core/src/gateway/state.rs` (`agent_summaries()` — добавить capabilities)
- Modify: `ui/src/types/api.generated.ts` (регенерация)

**Interfaces:**
- Produces:
  - `AgentDetailDto`: `pub profile: String` вместо `provider/model/provider_connection/fallback_provider/tts_provider/imagegen_provider` (удалить 6 полей).
  - Payload create/update: `profile: Option<String>` (default `"Default"` при create); шесть legacy-полей удаляются из схемы; PUT сохраняет `profile` из TOML при отсутствии в payload.
  - `AgentInfo`/summary: `pub capabilities: AgentCapabilitiesDto { text: bool, stt: bool, tts: bool, vision: bool, imagegen: bool, websearch: bool }` — из `profile_slots` работающего движка (`engine.cfg().profile_slots`), для незапущенных — резолвом по имени профиля.

- [ ] **Step 1:** Удалить поля из DTO/schema/crud (компилятор ведёт по списку: `merge_clearable_string` вызовы для `fallback_provider`/`tts_provider` в `crud.rs:698-699, 1288` — удалить), добавить `profile` и `AgentCapabilitiesDto` (новая структура с `#[cfg_attr(feature = "ts-gen", derive(ts_rs::TS))]` + `crate::register_ts_dto!`).

- [ ] **Step 2:** Обновить snapshot-тесты `agents/snapshots/*.snap` (`provider`/`model`/… исчезают, `profile` появляется) — перегенерировать insta-снапшоты (`cargo insta review` или удалить .snap и перезапустить тест — следовать существующему процессу; grep `insta` в dev-deps).

- [ ] **Step 3:** `make gen-types` → коммит `ui/src/types/api.generated.ts`. UI, использующий удалённые поля, чинится в Task 16-17 (до тех пор `cd ui && npx tsc --noEmit` будет красным — это ожидаемо в пределах Task 13-17, зафиксировать в сообщении коммита).

- [ ] **Step 4: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core agents`

```bash
git add crates/opex-core/src/gateway/handlers/agents/ crates/opex-core/src/gateway/state.rs ui/src/types/api.generated.ts
git commit -m "feat(profiles): agents API exposes profile + capabilities; drops per-agent provider fields (UI catches up in follow-ups)"
```

---

### Task 14: `provider_active` → только embedding; guard удаления провайдера по профилям

**Files:**
- Modify: `crates/opex-core/src/gateway/handlers/providers.rs:549-599` (list/set provider-active), `:410-440` (delete)

**Interfaces:**
- Produces:
  - `PUT /api/provider-active` с `capability != "embedding"` → 400 `{"error":"capability managed by profiles"}`.
  - `GET /api/provider-active` отдаёт только embedding-строки.
  - `DELETE /api/providers/{id}`: если имя используется в слотах какого-либо профиля → 409 `{"error":"provider_in_profiles","profiles":[…]}`.

- [ ] **Step 1: Failing-тест** (в тест-модуле providers.rs)

```rust
#[sqlx::test(migrations = "../../migrations")]
async fn provider_delete_blocked_when_referenced_by_profile(pool: sqlx::PgPool) {
    sqlx::query("INSERT INTO providers (name, type, provider_type) VALUES ('mm','tts','minimax')")
        .execute(&pool).await.unwrap();
    let mut slots = crate::db::profiles::Slots::new();
    slots.insert("tts".into(), vec![crate::db::profiles::SlotEntry { provider: "mm".into(), model: None, voice: None }]);
    crate::db::profiles::create_profile(&pool, "P", &slots).await.unwrap();
    let found = crate::db::profiles::profiles_referencing_provider(&pool, "mm").await.unwrap();
    assert_eq!(found, vec!["P".to_string()]);
}
```

и новая db-функция (в `db/profiles.rs`):

```rust
/// Имена профилей, в чьих слотах встречается провайдер `name` (JSONB containment по всем слотам).
pub async fn profiles_referencing_provider(db: &PgPool, name: &str) -> sqlx::Result<Vec<String>> {
    let rows: Vec<(String, serde_json::Value)> =
        sqlx::query_as("SELECT name, slots FROM profiles").fetch_all(db).await?;
    Ok(rows.into_iter().filter(|(_, slots)| {
        serde_json::from_value::<crate::db::profiles::Slots>(slots.clone()).unwrap_or_default()
            .values().flatten().any(|e| e.provider == name)
    }).map(|(n, _)| n).collect())
}
```

- [ ] **Step 2:** В `api_set_provider_active` первой строкой: `if input.capability != "embedding" { return 400 … }`. В `api_list_provider_active` — фильтр `capability == "embedding"`. В delete-handler перед удалением: `profiles_referencing_provider` → 409 при непустом.

- [ ] **Step 3: Проверка + Commit**

Run: `make check`; сервер/CI: `cargo test -p opex-core --bin opex-core providers`

```bash
git add crates/opex-core/src/gateway/handlers/providers.rs crates/opex-core/src/db/profiles.rs
git commit -m "feat(profiles): provider_active restricted to embedding; provider delete guarded by profile refs"
```

---

### Task 15: UI — хуки `useProfiles` + типы

**Files:**
- Create: `ui/src/hooks/use-profiles.ts`
- Test: `ui/src/hooks/__tests__/use-profiles.test.tsx`

**Interfaces:**
- Produces:

```ts
export interface SlotEntry { provider: string; model?: string; voice?: string }
export type ProfileSlots = Record<string, SlotEntry[]>;
export interface ProfileRow { id: string; name: string; slots: ProfileSlots; agents: string[]; created_at: string; updated_at: string }
export function useProfiles(): UseQueryResult<{ profiles: ProfileRow[] }>          // ["profiles"]
export function useCreateProfile(): UseMutationResult<…>   // POST /api/profiles
export function useUpdateProfile(): …                       // PUT /api/profiles/{id}
export function useCopyProfile(): …                         // POST /api/profiles/{id}/copy
export function useDeleteProfile(): …                        // DELETE /api/profiles/{id}
export const PROFILE_CAPABILITIES = ["text","compaction","stt","tts","vision","imagegen","websearch"] as const;
```

- [ ] **Step 1:** Реализация по образцу существующих хуков (`ui/src/hooks/queries.ts` / `useCommands` — те же `apiGet/apiPost/apiPut/apiDelete` из `@/lib/api`, инвалидация `["profiles"]` в `onSuccess`).

- [ ] **Step 2:** Тест — мок fetch по образцу существующих hook-тестов (например `ChatComposer.voice-settings.test.tsx` mocking-паттерн): `useProfiles` парсит список; мутация инвалидирует ключ.

- [ ] **Step 3: Проверка + Commit**

Run: `cd ui && npm test -- use-profiles`
Expected: PASS.

```bash
git add ui/src/hooks/use-profiles.ts ui/src/hooks/__tests__/use-profiles.test.tsx
git commit -m "feat(profiles-ui): useProfiles query/mutation hooks"
```

---

### Task 16: UI — страница «Профили» + пункт меню

**Files:**
- Create: `ui/src/app/(authenticated)/profiles/page.tsx`
- Create: `ui/src/app/(authenticated)/profiles/_parts/ProfileEditor.tsx`
- Modify: `ui/src/components/app-sidebar.tsx:77` (вставить пункт ПЕРЕД `nav.providers`)
- Modify: `ui/src/i18n/locales/ru.json`, `ui/src/i18n/locales/en.json`
- Test: `ui/src/app/(authenticated)/profiles/__tests__/profile-editor.test.tsx`

**Interfaces:**
- Consumes: Task 15 хуки; `/api/providers` список (существующий hook провайдеров — grep `useProviders` в ui/src/hooks), `/api/tts/voices?provider=` (fetch-паттерн из `providers/page.tsx:174-186`).
- Produces: роут `/profiles/`, i18n-ключи `nav.profiles`, `profiles.*`.

- [ ] **Step 1: Sidebar**

```tsx
      { labelKey: "nav.profiles", href: "/profiles/", icon: Layers },
      { labelKey: "nav.providers", href: "/providers/", icon: Zap },
```

(`Layers` — из `lucide-react`, добавить в импорт.)

- [ ] **Step 2: Страница-список** (`page.tsx`): карточки профилей из `useProfiles()` — имя, по строке на непустой слот (`{cap}: {chain[0].provider}{voice ? ` (${voice})` : ""}{chain.length>1 ? ` +${chain.length-1}` : ""}`), бейджи `agents`, кнопки: Создать (prompt имени → `useCreateProfile` с пустыми slots → открыть редактор), Копировать (`useCopyProfile`), Удалить (`useDeleteProfile`; `disabled={p.agents.length>0 || p.name==="Default"}` c title-подсказкой). Клик по карточке открывает `ProfileEditor` (диалог). Мобильная вёрстка: `min-w-0 overflow-hidden` на Card (эталон — tools/page renderYamlCard).

- [ ] **Step 3: Редактор** (`ProfileEditor.tsx`): для каждого cap из `PROFILE_CAPABILITIES` секция:
  - селект провайдера (записи `providers` с категорией cap; для text/compaction — категории text|llm) + кнопка «+ резерв» (добавляет строку), стрелки ↑/↓ (swap элементов массива), удалить строку;
  - для text/compaction/vision: `<Input>` model на строку;
  - для tts: селект голоса на строку (fetch `/api/tts/voices?provider=${entry.provider}` при выборе провайдера, локальный state по образцу `providers/page.tsx:94-186`);
  - Save → `useUpdateProfile({id, slots})`; ошибки 400 → toast.
  Имя редактируется `<Input>` (disabled для Default).

- [ ] **Step 4: i18n** — добавить в оба словаря: `nav.profiles` («Профили»/"Profiles"), `profiles.title`, `profiles.create`, `profiles.copy`, `profiles.delete`, `profiles.in_use_hint`, `profiles.add_reserve`, `profiles.model_placeholder`, `profiles.voice_placeholder`, `profiles.saved`, `profiles.slot_{text,compaction,stt,tts,vision,imagegen,websearch}`.

- [ ] **Step 5: Тест** — рендер редактора с мокнутыми хуками: добавление резерва даёт вторую строку; ↑ меняет порядок; Save вызывает mutate с ожидаемыми slots.

- [ ] **Step 6: Проверка + Commit**

Run: `cd ui && npm test -- profile-editor && npx tsc --noEmit | head -30`
Expected: тест PASS (tsc может оставаться красным из-за agent-dialog до Task 17).

```bash
git add ui/src/app/\(authenticated\)/profiles ui/src/components/app-sidebar.tsx ui/src/i18n/locales
git commit -m "feat(profiles-ui): Profiles page (list/editor/copy) + sidebar entry above Providers"
```

---

### Task 17: UI — диалог агента: селект профиля вместо блока провайдеров

**Files:**
- Modify: `ui/src/app/(authenticated)/agents/AgentEditDialog.tsx` (поля form-state `provider/model/provider_connection/fallback_provider/tts_provider` и их разметка, в т.ч. блок `:420-440`)
- Modify: `ui/src/app/(authenticated)/agents/page.tsx` (payload create/update)
- Modify: `ui/src/app/(authenticated)/agents/__tests__/agent-form.test.tsx`, `agent-tabs.test.tsx` (упоминания старых полей)

- [ ] **Step 1:** В form-state диалога заменить шесть полей одним `profile: string`; разметка: `<Select>` из `useProfiles()` (+ пункт-ссылка «Управлять профилями» → `/profiles/`). Убрать селекты provider/model/fallback/TTS (блок `:420-440`) и связанные `voices`-пропсы, если они использовались только там.

- [ ] **Step 2:** Payload create/update агента: `{ name, language, profile, temperature, … }` — без удалённых полей (типы из `api.generated.ts` уже требуют этого после Task 13).

- [ ] **Step 3:** Починить/обновить тесты диалога (grep `tts_provider|provider_connection|fallback_provider` в `ui/src/app/(authenticated)/agents/__tests__`).

- [ ] **Step 4: Проверка + Commit**

Run: `cd ui && npm test -- agent && npx tsc --noEmit`
Expected: тесты PASS; tsc чистый ПОСЛЕ этой задачи (осталась только providers-страница — Task 18 использует ещё существующие API).

```bash
git add ui/src/app/\(authenticated\)/agents
git commit -m "feat(profiles-ui): agent dialog selects profile instead of provider fields"
```

---

### Task 18: UI — страница Провайдеры: «Активные» → блок Embedding

**Files:**
- Modify: `ui/src/app/(authenticated)/providers/page.tsx` (per-capability active groups, `:40-70, 100-130, 317+`)
- Modify: тесты страницы (grep `provider-active|active_updated` в ui/src)

- [ ] **Step 1:** Удалить UI-группы активных для всех capability, кроме `embedding`: перечень capability-групп сузить до `["embedding"]` (существующий механизм `handleActiveChange`/`PUT /api/provider-active` сохранить только для него; заголовок секции — новая i18n-строка `providers.embedding_section`). CRUD записей провайдеров не трогать.

- [ ] **Step 2:** Обработать 409 при удалении провайдера (`provider_in_profiles`): toast с перечислением профилей (`providers.delete_in_profiles`).

- [ ] **Step 3: Проверка + Commit**

Run: `cd ui && npm test && npx tsc --noEmit`

```bash
git add ui/src/app/\(authenticated\)/providers ui/src/i18n/locales
git commit -m "feat(profiles-ui): providers page keeps only Embedding active section"
```

---

### Task 19: Setup wizard — наполнение Default-профиля

**Files:**
- Modify: `ui/src/app/setup/page.tsx:267-347` (`doStep1`, `doStep2`)
- Modify: `ui/src/app/setup/__tests__/setup-page.test.tsx`

- [ ] **Step 1:** В `doStep1` после успешного test-call (перед `setStep("agent")`): найти Default и записать text-слот:

```tsx
      // profiles: наполняем Default text-слотом созданного провайдера
      try {
        const { profiles } = await apiGet<{ profiles: Array<{ id: string; name: string; slots: Record<string, unknown> }> }>("/api/profiles");
        const def = profiles.find((p) => p.name === "Default");
        if (def) {
          await apiPut(`/api/profiles/${def.id}`, {
            slots: { ...def.slots, text: [{ provider: providerNameRef.current, model: defaultModel.trim() }] },
          });
        }
      } catch { /* seed создаст/поправит при рестарте; не блокируем wizard */ }
```

(`apiPut` — тот же модуль, что `apiPost`/`apiGet` в этом файле.)

- [ ] **Step 2:** `doStep2` payload: `{ name, language: agentLang, profile: "Default", temperature: 1.0 }` — удалить `provider/model/provider_connection`.

- [ ] **Step 3:** Обновить setup-тест (grep `provider_connection` в `setup-page.test.tsx`).

- [ ] **Step 4: Проверка + Commit**

Run: `cd ui && npm test -- setup`

```bash
git add ui/src/app/setup
git commit -m "feat(profiles-ui): wizard fills Default profile text slot; agent created with profile"
```

---

### Task 20: Финальная верификация

**Files:** нет новых.

- [ ] **Step 1:** Полный локальный статический прогон: `make check && make lint && cd ui && npx tsc --noEmit && npm run build && cd ../toolgate && python -m pytest -q`
- [ ] **Step 2:** `make gen-types` — диff `api.generated.ts` пуст (или закоммитить).
- [ ] **Step 3:** Серверный прогон Rust-тестов: `make test-db` (или на сервере — см. память `reference_server_rust_test_gotcha`: `CARGO_BUILD_JOBS=4 nice ionice`, тесты в BIN-таргете).
- [ ] **Step 4:** Ручной E2E-чеклист на проде после деплоя:
  - миграция seed прошла: `profiles` содержит Default (+пер-агентные), агентские TOML переписаны, `provider_active` — только embedding;
  - чат каждого агента работает (text из профиля), fallback-цепочка: временно сломать primary → своп в логах;
  - `synthesize_speech` шлёт голос профиля; `/api/tts/synthesize?agent=X` — 200, для профиля без tts — 409;
  - `search_web` работает; UI: страница Профили, копирование, назначение агенту, hot-reload после PUT.
- [ ] **Step 5:** Commit остатков + запись в память о деплое.

---

## Порядок и зависимости

```text
T1 (db) → T2 (resolver) → T3 (config) → T4 (seed)
T5 (engine primary/compaction) ← T2,T3
T6 (fallback chain) ← T5
T7 (capability tools) ← T5
T8 (media chain) ← T5
T9 (tts synthesize) ← T2
T10 (toolgate) — независим, но до E2E T20
T11 (search header) ← T5, T10
T12 (profiles API) ← T1
T13 (agents API) ← T12
T14 (provider-active/delete guard) ← T1
T15-T19 (UI) ← T12-T14
T20 — финал
```

Замечание для исполнителя: после T13 и до конца T17 `tsc` в ui/ красный — это запланированное состояние внутри батча; не пытаться «чинить» откатом типов.
